<h1><%= @song.title %></h1>

<h3><%= link_to @song.artist_name, artist_path(@song.artist) %></h3>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, song_path(song) %></li>
  <% end %>
</ul>
